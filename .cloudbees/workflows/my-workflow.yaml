apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
# wf-p06 test comment

on:
  workflow_dispatch:

jobs:
  my-edge-job:
    with:
      script: |
        echo "Helloo world!! I'm $(whoami) on $(hostname)."
      labels: "edge-run"
    delegates: cloudbees-io/edge-custom-job/custom-job.yaml